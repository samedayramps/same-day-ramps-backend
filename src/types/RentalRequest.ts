// src/types/RentalRequest.ts

export enum SalesStage {
  RENTAL_REQUEST = 'Rental Request',
  CONTACTED = 'Contacted',
  QUOTE_SENT = 'Quote Sent',
  JOB_CREATED = 'Job Created', // Added this value
  // ... other stages as needed
}